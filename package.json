{
  "name": "dcampaigns",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Commissioning creative work — brief, assign, deliver, decide — as a distributed application over a signed graph",
  "scripts": {
    "sync-engine": "rm -rf public/genosdb && mkdir -p public && cp -R node_modules/genosdb/dist public/genosdb",
    "dev": "pnpm sync-engine && vite",
    "build": "pnpm sync-engine && vite build",
    "preview": "vite preview",
    "test": "playwright test",
    "test:ui": "playwright test --ui"
  },
  "dependencies": {
    "genosdb": "^0.25.0"
  },
  "devDependencies": {
    "@playwright/test": "1.62.1",
    "vite": "^7.1.2"
  },
  "author": "Esteban Fuster Pozzi (@estebanrfp)",
  "license": "MIT"
}
