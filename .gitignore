node_modules/
dist/
dist-ssr/
.DS_Store
*.local
.env
.env.*
!.env.example

# Session config, not project config
.claude/

# Playwright
test-results/
playwright-report/

# Engine copied verbatim at build time — never edited, never committed
public/genosdb/
